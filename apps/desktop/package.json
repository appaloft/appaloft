{
  "name": "@yundu/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run prepare:sidecar && bunx @tauri-apps/cli dev",
    "package": "bun run prepare:sidecar && bunx @tauri-apps/cli build",
    "package:app": "bun run prepare:sidecar && bunx @tauri-apps/cli build --bundles app",
    "prepare:sidecar": "bun run scripts/prepare-sidecar.ts"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
