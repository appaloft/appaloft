/**
 * Auto-generated / maintained by boneyard-js.
 * Run `bunx boneyard-js build` (or the Vite plugin in dev) to capture bones
 * from named `<Skeleton>` wrappers into `*.bones.json`, then re-export here.
 *
 * Until bones are captured for a name, the Skeleton falls back to its
 * `fallback` snippet (if provided) or renders children when not loading.
 */
import { registerBones } from "boneyard-js";

// Intentionally empty until `boneyard-js build` captures layouts.
// Example after capture:
// import deploymentsPage from "./deployments-page.bones.json";
// registerBones({ "deployments-page": deploymentsPage });
registerBones({});
