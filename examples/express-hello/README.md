# express-hello

Minimal Express demo application for local Appaloft smoke validation.

Use it to verify:

- `workspace-commands` local host-process deployments
- `dockerfile` local Docker deployments

The app exposes:

- `GET /`
- `GET /health`
