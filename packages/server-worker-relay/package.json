{
  "name": "@appaloft/server-worker-relay",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@appaloft/core": "workspace:*"
  },
  "scripts": {
    "lint": "oxlint .",
    "typecheck": "tsc -p tsconfig.json --noEmit --checkers 4",
    "test": "bun test --timeout=30000 --max-concurrency=1",
    "test:unit": "bun test --timeout=30000 --max-concurrency=1"
  }
}
