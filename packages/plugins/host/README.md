# @appaloft/plugin-host

Plugin loading and registry package.

Responsibilities:

- load local plugin definitions
- validate compatibility
- expose plugin discovery
- expose system-plugin web extensions
- expose system-plugin backend route and middleware registrations
