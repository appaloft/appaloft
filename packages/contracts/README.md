# @appaloft/contracts

Stable transport and tool contracts.

Responsibilities:

- HTTP request and response schemas
- MCP tool contracts
- public DTOs shared by CLI formatting and Web API clients

Depends on:

- schema libraries only

Must not:

- import core entities or infrastructure code
