export function occupancyAgentWakeLine(name: string): string {
  return `Waking agent ${name}`;
}

export function occupancyAgentWokeLine(name: string): string {
  return `Woke agent ${name} — your work is on its disk`;
}
