# @yundu/adapter-cli

CLI adapter for Yundu.

Responsibilities:

- parse commands and flags
- call application use cases
- render operational output for humans and automation

Must not:

- own deployment logic independent of the application layer
