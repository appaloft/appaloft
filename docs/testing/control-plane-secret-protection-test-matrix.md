# Control-Plane Secret Protection Test Matrix

| ID | Scenario | Surface | Automation | Binding | Status |
| --- | --- | --- | --- | --- | --- |
| CPS-PROTECT-001 | Normal protect/unprotect and deployment materialization. | crypto/application/runtime | unit + integration | `control-plane-secret-protector.test.ts`; environment/resource lifecycle; deployment create/plan; dependency stores | passing |
| CPS-FAIL-002 | Missing keyring blocks plan and execution. | application/runtime | integration | `create-deployment.test.ts`; `deployment-plan-preview.test.ts` | passing |
| CPS-FAIL-003 | Wrong key fails authentication without fallback. | crypto/application | unit + integration | `control-plane-secret-protector.test.ts`; plan/runtime materialization tests | passing |
| CPS-FAIL-004 | Corrupt envelope blocks the whole operation. | crypto/application | unit + integration | `control-plane-secret-protector.test.ts`; plan/runtime materialization tests | passing |
| CPS-FAIL-005 | One invalid variable prevents partial runtime env. | runtime adapters | integration | `dependency-runtime-secrets.test.ts` | passing |
| CPS-ROTATE-006 | Old-to-new rotation is atomic and decrypt-compatible. | PG/PGlite | integration | `control-plane-secret-rotation.pglite.test.ts` | passing |
| CPS-ROTATE-007 | Mid-transaction failure rolls back; retry is idempotent. | PG/PGlite | integration | `control-plane-secret-rotation.pglite.test.ts` | passing |
| CPS-ROTATE-008 | Legacy plaintext requires explicit migration and backup ref. | PG/PGlite + CLI | integration | rotation PGlite tests and source CLI smoke | passing |
| CPS-SUBSTRATE-009 | Docker, Compose, SSH Compose, and Swarm preserve all runtime keys. | runtime adapters | integration + real Docker/Swarm | runtime command/readback tests; Swarm fake and opt-in real execution; local Docker/Compose smoke | passing |
| CPS-PROOF-010 | Missing observed runtime keys cannot be reported as matching/successful. | deployment proof/Web/API | integration | `deployment-proof-evidence.test.ts`; application and transport proof tests | passing |
| CPS-SAFE-011 | Logs, errors, API, CLI, diagnostics, and proof leak no secret/cipher/key material. | published surfaces | integration | protector, runtime redaction, proof evidence, CLI smoke, transport tests, source scan | passing |
| CPS-COMPAT-012 | Retained old key/version decrypts and rewraps to active. | crypto/PG | unit + integration | protector retained-key test and rotation PGlite tests | passing |
| CPS-REMOTE-013 | SSH PGlite rotation plan leaves durable remote files and schema/backend markers unchanged after releasing its ephemeral lock; apply retains guarded sync semantics. | shell/CLI/SSH PGlite | integration | `deployment-ssh-remote-state.test.ts`; `remote-pglite-state-sync.test.ts`; source CLI help smoke | passing |
| CPS-DIAG-014 | Unreadable plan findings are bounded, actionable, and value/ciphertext free. | PG/PGlite + CLI | integration | `control-plane-secret-rotation.pglite.test.ts`; source leak assertion | passing |
| CPS-REMOTE-015 | SSH PGlite mirror download and upload use private temporary archive files instead of buffering the complete compressed state in process memory. | shell/CLI/SSH PGlite | integration | `remote-pglite-state-sync.test.ts` file-sink, file-source, private-mode, and failure-cleanup tests | passing |
| CPS-SAFE-016 | Opt-in machine-readable CLI errors expose only a versioned classification allowlist and omit arbitrary error payloads. | shell/CLI | unit + integration | `runtime-error-format.test.ts`; shell pre-composition formatter integration | passing |
| CPS-COMPAT-017 | Pre-migration rotation planning treats absent post-initial secret-bearing tables as empty while preserving fail-closed reads. | PG/PGlite | integration | `control-plane-secret-rotation.pglite.test.ts` legacy-schema fixture | passing |
| CPS-DIAG-018 | Rotation source read failures expose only a fixed source reason without database details. | PG/PGlite + CLI safe error | integration | `control-plane-secret-rotation.pglite.test.ts` source-failure fixture; `CPS-SAFE-016` renderer | passing |
| CPS-COMPAT-019 | Optional post-initial sources use direct reads; exact PostgreSQL `42P01` is empty while all other failures remain safe and fail closed. | PG/PGlite | integration | `control-plane-secret-rotation.pglite.test.ts` migration-026 fixture and injected optional-source failure | passing |
| CPS-COMPAT-020 | A fresh or partially initialized state can be planned before the initial schema; exact PostgreSQL `42P01` is empty for every rotation source while all other failures remain fail closed. | PG/PGlite | integration | `control-plane-secret-rotation.pglite.test.ts` pre-initial PGlite fixture and source-failure fixture | passing |
| CPS-DIAG-021 | Rotation source failures map a bounded SQLSTATE allowlist to fixed operational categories, traverse only a bounded cause chain, and never publish the SQLSTATE or database details. | PG/PGlite + CLI safe error | integration | `control-plane-secret-rotation.pglite.test.ts` schema-incompatible and nested undefined-table fixtures; `CPS-SAFE-016` renderer | passing |
| CPS-DIAG-022 | Known driver wrapper fields and SQLSTATE aliases are traversed with bounded depth/node count and publish only the existing fixed operational categories. | PG/PGlite + CLI safe error | integration | `control-plane-secret-rotation.pglite.test.ts` nested `error`/`originalError`/`sqlState` fixture; `CPS-SAFE-016` renderer | passing |
| CPS-DIAG-023 | Allowlisted PostgreSQL SQLSTATE classes map to fixed safe categories while exact `42P01` remains the only empty-source signal. | PG/PGlite + CLI safe error | integration | `control-plane-secret-rotation.pglite.test.ts` class `42` operator fixture and no-detail assertions; `CPS-SAFE-016` renderer | passing |
| CPS-COMPAT-024 | Environment and Resource secret filters avoid embedded-runtime boolean bind inference without broadening the selected rows. | PG/PGlite | integration | Historical compiler assertion for `IS TRUE` with no `$1`; live preflight remained generic and CPS-COMPAT-025 supersedes the predicate | superseded |
| CPS-COMPAT-025 | Environment and Resource source reads avoid database boolean predicates while non-secret rows remain outside inspection and plan accounting. | PG/PGlite | integration | `control-plane-secret-rotation.pglite.test.ts` captures both compiled source queries, asserts no `WHERE`, and seeds secret/non-secret rows to prove only the secret row is counted | passing |
| CPS-DIAG-026 | Generic Environment source failures are narrowed by bounded read-only single-column probes without publishing values, row counts, SQL, or arbitrary database details. | PG/PGlite + CLI safe error | integration | `control-plane-secret-rotation.pglite.test.ts` injected combined-row failure, fixed row-materialization category, and private-detail absence assertion | passing |
| CPS-DIAG-027 | Bounded source or column probes classify additional PostgreSQL SQLSTATE families into fixed safe operational categories without publishing codes, values, SQL, or database details. | PG/PGlite + CLI safe error | integration | `control-plane-secret-rotation.pglite.test.ts` injected `22` class failure and no-detail assertions; `CPS-SAFE-016` renderer | passing |
