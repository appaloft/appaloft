# Plan: Remote MCP Access For Agent Workspaces

1. Extend the Adapter SDK with bounded remote MCP requirements and conformance failures.
2. Add Profile installation MCP references, compiler validation, safe read models, and one configure operation.
3. Add the provider-neutral MCP access issuer/revoker port and immutable Runtime binding snapshot.
4. Compose Pi and OpenCode native configuration through the same harness contract.
5. Revoke exact Runtime grants across completion, cancellation, replacement, pause/terminate cleanup.
6. Add operation catalog, HTTP/oRPC, generated SDK, CLI, MCP metadata, docs, and tests.
7. Run public checks and an independent public/private boundary review before Cloud pins public main.
