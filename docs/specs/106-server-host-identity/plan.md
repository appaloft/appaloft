# Plan

1. Record the host identity and duplicate policy in ADR-090 and server operation specs.
2. Add failing value-object, registration, and runtime-adapter tests.
3. Implement canonical identity, duplicate admission, safe display, and SSH failure classification.
4. Run focused tests, package type checks, docs checks, and repository review.
