# Plan: Storage Volume Backup Automation

- Add application-owned policy repository/read model and configure/list/show operations.
- Add scheduled and pre-deploy orchestration over existing command-bus operations.
- Add verify-first retention selection and optional notification port.
- Persist policy/run readback in PG/PGlite and expose CLI, HTTP/oRPC, Web, docs, and worker config.
- Bind tests to `STOR-BACKUP-AUTO-*` ids before marking implementation complete.
