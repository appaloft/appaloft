# Plan

1. Accept ADR-099 and synchronize operation, workflow and test-matrix contracts.
2. Extend Snapshot state with neutral reason and recovery compatibility observations.
3. Add the lifecycle policy port, static implementation, maintenance scheduling and retention.
4. Gate terminate/expiry with required or best-effort reusable capture.
5. Add portable reusable Docker packages and compatible cross-provider provisioning.
6. Compose hosted policy and registered-Server routing without leaking topology.
7. Run focused, repository and real Docker acceptance before two-repository delivery.
