# Read Model Count Queries Tasks

- [x] Add public count operation source-of-truth rows in `docs/CORE_OPERATIONS.md`.
- [x] Add count query vertical slices for projects, servers, environments, resources, dependency
  resources, and deployments.
- [x] Add `count` methods to read model ports and memory/SQL adapters.
- [x] Register count query services and expose them through oRPC.
- [x] Switch the home dashboard totals from list calls to count calls.
- [x] Cover read-model delegation with `READ-MODEL-COUNT-001`.
- [x] Cover deployment count filters with `READ-MODEL-COUNT-002`.
- [x] Cover HTTP/oRPC dispatch with `READ-MODEL-COUNT-003`.
- [x] Cover home page count-vs-list usage with `HOME-COUNT-QUERY-001` and
  `HOME-COUNT-QUERY-002`.
