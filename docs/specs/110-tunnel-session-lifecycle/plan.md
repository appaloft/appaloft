# Plan: Tunnel Session Lifecycle

- Add application-owned session state, provider registry port, and start/list/show/revoke operations.
- Add Cloudflare Quick Tunnel and ngrok local-agent adapters with redacted process handling.
- Add disabled-by-default expiry/orphan reconciler, persistence, API/CLI/Web, docs, and tests.
